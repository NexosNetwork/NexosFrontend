specVersion: "v1beta"
serviceId: "seed - Copy"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "seed - Copy-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
